export PETSC_DIR=/usr/local/src/petsc/petsc-3.2-p7
export FSPL_DIR=$HOME/CFD_codes/EulFS.3.2.3
export PETSC_ARCH=linux_intel8.1-opt

echo `which gcc`
echo
echo `which gfortran`
echo

echo PETSC_DIR is $PETSC_DIR
echo
echo PETSC_ARCH is $PETSC_ARCH

sleep 2

make install
