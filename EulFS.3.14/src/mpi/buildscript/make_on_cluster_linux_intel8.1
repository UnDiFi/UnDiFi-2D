export PETSC_DIR=/usr/local/src/petsc/petsc-3.2-p7
export FSPL_DIR=$HOME/CFD_codes/EulFS.3.2.3
export PETSC_ARCH=linux_intel8.1

echo `which gcc`
echo
echo `which gfortran`

echo $PETSC_DIR
echo $PETSC_ARCH

sleep 2

make install
